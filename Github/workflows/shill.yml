name: Shill-Engine
on:
  schedule:
    - cron: '0 * * * *' # Runs every hour
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep-Alive
        run: curl -s https://[Moxbig].github.io/[caper-drop]/
